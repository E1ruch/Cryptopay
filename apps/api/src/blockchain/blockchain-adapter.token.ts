/** The raw `ReadonlyMap<network, BlockchainAdapter>` from `@cryptopay/blockchain` — inject `BlockchainAdapterRegistry` instead unless you need the map itself. */
export const BLOCKCHAIN_ADAPTER_REGISTRY = Symbol('BLOCKCHAIN_ADAPTER_REGISTRY');
